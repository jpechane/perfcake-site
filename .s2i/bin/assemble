#!/bin/bash
rm -rf /tmp/site-source
mkdir -p /tmp/site-source
cd /tmp/site-source

git clone https://github.com/jpechane/Web perfcake
cd perfcake
awestruct
cp -R _site/* /apache2/www
cd ..

rm -rf /tmp/site-source
